# Local build output
_site/
.cache/

# Editor / OS
.DS_Store
*.swp

# AI consistency cache (regenerated by scripts/check_consistency.rb; not for git)
cache/

# Node (kept off the repo for now)
node_modules/
web/dist/
web/node_modules/
web/.vite-ssg-temp/
