# 15: Conflicts (`/conflicts/`)

Port `web/src/pages/conflicts.vue` → `src/pages/conflicts.astro`:
- Sticky edition filter
- What is an ID conflict card
- Per-edition conflicting IDs tables (ID, distinct concepts, clickable pub links)
- Resolution guidance card
