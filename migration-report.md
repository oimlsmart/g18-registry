# Migration Report

Generated by `<repo>/scripts/migrate_from_vocab.rb` against
`/Users/mulgogi/src/oimlsmart/vocab/datasets`.

## Editions consumed

- 202X (primary): 2814 source concepts
- 2010: 2125 source concepts

## Counts

| Metric | Value |
|---|---|
| Total source entries parsed | 4939 |
| Unique-term files written | 1731 |
| Distinct canonicalized terms | 1731 |
| Total publication instances (sum of `publications[]`) | 4939 |
| Per-instance VIM/VIML edges preserved (`publications[*].related`) | 100 |
| Distinct term-level edges (each term's `related`, deduplicated) | 56 |
| Designation annotations stripped | 43 |
| Alias merges applied | 9 |
| ID conflicts across editions | 0 |

## Invariants

- ✓ 1731 unique-term files written (one per distinct canonicalized term)
- ✗ 100 per-instance VIM/VIML edges preserved on `publications[*].related` (expected 101)
- ✓ 56 distinct term-level edges (deduplicated superset in each term's `related`)

> Note: the SOURCE_INSTANCE_COUNT invariant was relaxed when we
> added multi-edition support (2010 + 202X). The migration now
> preserves all source concepts across all editions; the count
> grows as new editions are added.

## ID conflicts (0 across 0 editions)

IDs in the source publication that are reused for two or more
distinct concepts. Editors (TC 1) must decide whether the concepts
are truly distinct (split via `<id>a` / `<id>b`) or represent a
single concept (merge).

_(none)_

## Designation annotations stripped (43)

Source designations sometimes include trailing editorial notes in
square brackets (VIM citations, scope restrictions, footnote
markers). The migration strips these before grouping so the term
merges with its canonical form. Parentheses are preserved (they
hold legitimate symbol abbreviations like `(Max)`, `(MPE)`).

- `T1 error` → `T1 error`
- `T2 error` → `T2 error`
- `absorbance (A = lg(1/τ))` → `absorbance (A = lg(1/τ))`
- `accuracy; measurement accuracy [VIM 2.13]` → `accuracy; measurement accuracy`
- `adjustment [VIM 3.11]` → `adjustment`
- `audit trail [OIML D 31, 3.1.1]` → `audit trail`
- `calibration [VIM 2.39]` → `calibration`
- `certified reference material; (CRM) [VIM 5.14]` → `certified reference material; (CRM)`
- `checking facility (OIML V 1 [1], 5.07)` → `checking facility (OIML V 1`
- `cryptographic means [OIML D 31, 3.1.8]` → `cryptographic means`
- `disturbance (OIML V 1 [1], 5.19)` → `disturbance (OIML V 1`
- `durability error (OIML V 1 [1], 5.16)` → `durability error (OIML V 1`
- `error of indication / measurement error (OIML V 1 [1], 0.04)` → `error of indication / measurement error (OIML V 1`
- `fault (OIML V 1 [1], 5.12)` → `fault (OIML V 1`
- `fault [OIML D 11, 3.10]` → `fault`
- `influence factor (OIML V 1 [1], 5.18)` → `influence factor (OIML V 1`
- `influence quantity (OIML V 1 [1], 0.07)` → `influence quantity (OIML V 1`
- `initial intrinsic error (OIML V 1 [1], 5.11)` → `initial intrinsic error (OIML V 1`
- `intrinsic error (OIML V 1 [1], 0.06)` → `intrinsic error (OIML V 1`
- `intrinsic error [OIML D 11, 3.8]` → `intrinsic error`
- `legally relevant [OIML D 31, 3.1.25]` → `legally relevant`
- `legally relevant parameter [OIML D 31, 3.1.26]` → `legally relevant parameter`
- `legally relevant software part [OIML D 31, 3.1.27]` → `legally relevant software part`
- `maximum permissible error (MPE) (maximum permissible error, limit of error) [VIM 4.26]` → `maximum permissible error (MPE) (maximum permissible error, limit of error)`
- `maximum permissible error / MPE (OIML V 1 [1], 0.05)` → `maximum permissible error / MPE (OIML V 1`
- `measurement error (error of measurement, error) [VIM 2.16]` → `measurement error (error of measurement, error)`
- `measurement repeatability (repeatability) [VIM, 2.21]` → `measurement repeatability (repeatability)`
- `measurement reproducibility (reproducibility) [VIM 2.25]` → `measurement reproducibility (reproducibility)`
- `rated operating conditio [VIM 4.9]` → `rated operating conditio`
- `reference condition [VIM 4.11]` → `reference condition`
- `reference conditions [VIM:1993, 5.7 [1]]` → `reference conditions`
- `reference quantity value; reference value [VIM 5.18]` → `reference quantity value; reference value`
- `repeatability condition of measurement (repeatability condtion) [VIM 2.20]` → `repeatability condition of measurement (repeatability condtion)`
- `reproducibility condition of measurement (reproducibility condition) [VIM 2.24]` → `reproducibility condition of measurement (reproducibility condition)`
- `significant fault (OIML V 1 [1], 5.14)` → `significant fault (OIML V 1`
- `software examination [OIML D 31, 3.1.47]` → `software examination`
- `specific molar absorption coefficient (ε = A/bc)` → `specific molar absorption coefficient (ε = A/bc)`
- `transmittance (τ = Φtr / Φ0)` → `transmittance (τ = Φtr / Φ0)`
- `type (pattern) evaluation [VIML, 2.04]` → `type (pattern) evaluation`
- `type approval [VIML 2.05]` → `type approval`
- `universal  device [OIML D 31, 3.1.59]` → `universal  device`
- `verification of a measuring instrument [VIML, 2.09]` → `verification of a measuring instrument`
- `weigh length (L) [not applicable to belt weighers inclusive of conveyor]` → `weigh length (L)`

## Alias merges (9)

Editorial variants merged via `tc-sc/term-aliases.yaml`. Extend
that file to handle additional singular/plural or other variants.

- `characteristic partial internal absorbance Ac (Ac = lg(Φr / Φs) = εbc)` ← `characteristic partial internal absorbance Ac (Ac = lg(stem:[Phi]r / stem:[Phi]s) = stem:[epsilon]bc)`
- `error (of indication)` ← `error of indication(stem:[E])`
- `fault` ← `fault stem:[\Delta e]`
- `minimum measured quantity (MMQ)` ← `minimum measured quantityMMQ(stem:[V_{\min}])`
- `net absorbance, ΔA` ← `net absorbance,stem:[\Delta A]`
- `rated operating conditions` ← `rated operating condition`
- `reference conditions` ← `reference condition`
- `specific absorbance, kλ` ← `specific absorbance,stem:[k_{\lambda}]`
- `specific net absorbance, Δk` ← `specific net absorbance,stem:[\Delta k]`

## Multi-edge terms (7)

Terms whose instances reference more than one VIM/VIML concept. The
first edge (lowest G 18 identifier, then dedup order) populates
`data.official_concept`; all edges are preserved under `related`.

- `conventional true value (of a quantity)`
- `error (of indication)`
- `intrinsic error`
- `rated operating conditions`
- `reference conditions`
- `repeatability`
- `repeatability error`

## Slug collisions (67)

Distinct canonicalized terms whose slugified form collides. The
alphabetically-first term keeps the bare slug; subsequent colliders
get a `-<g18-id>` suffix so every term gets its own file. Editors
should review these and consider adding alias entries when the
variants represent the same concept.

- `absorbed-dose-working-range` ← `absorbed dose working range`, `absorbed-dose working range` (colliders get a `-<g18-id>` suffix)
- `accuracy-measurement-accuracy` ← `accuracy measurement accuracy`, `accuracy; measurement accuracy` (colliders get a `-<g18-id>` suffix)
- `analysis-wavelength` ← `analysis wavelength`, `analysis wavelength, ` (colliders get a `-<g18-id>` suffix)
- `automatic-zero-setting-device` ← `automatic zero setting device`, `automatic zero-setting device` (colliders get a `-<g18-id>` suffix)
- `calibration-equation-calibration` ← `calibration equation calibration`, `calibration equation; calibration` (colliders get a `-<g18-id>` suffix)
- `certified-reference-material-crm` ← `certified reference material (crm)`, `certified reference material crm`, `certified reference material; (crm)` (colliders get a `-<g18-id>` suffix)
- `device-specific-parameter` ← `device specific parameter`, `device-specific parameter` (colliders get a `-<g18-id>` suffix)
- `equipment-under-test-eut` ← `equipment under test (eut)`, `equipment under test eut` (colliders get a `-<g18-id>` suffix)
- `error-of-indication` ← `error (of indication)`, `error of indication` (colliders get a `-<g18-id>` suffix)
- `event-counter` ← `event counter`, `event-counter` (colliders get a `-<g18-id>` suffix)
- `flow-rate-q` ← `flow rate q`, `flow rate, q` (colliders get a `-<g18-id>` suffix)
- `full-draught-weighing` ← `full draught weighing`, `full-draught weighing` (colliders get a `-<g18-id>` suffix)
- `indicated-volume-vi` ← `indicated volume vi`, `indicated volume, vi` (colliders get a `-<g18-id>` suffix)
- `indication-of-a-measuring-instrument` ← `indication (of a measuring instrument)`, `indication of a measuring instrument` (colliders get a `-<g18-id>` suffix)
- `initial-zero-setting-device` ← `initial zero setting device`, `initial zero-setting device` (colliders get a `-<g18-id>` suffix)
- `instrumental-drift` ← `(instrumental) drift`, `instrumental drift` (colliders get a `-<g18-id>` suffix)
- `integrity-of-programs-data-or-parameters` ← `integrity (of programs, data, or parameters)`, `integrity of programs, data or parameters` (colliders get a `-<g18-id>` suffix)
- `load-measuring-device` ← `load measuring device`, `load-measuring  device` (colliders get a `-<g18-id>` suffix)
- `load-transmitting-device` ← `load transmitting device`, `load-transmitting device` (colliders get a `-<g18-id>` suffix)
- `manufacturer-s-representative` ← `manufacturer's representative`, `manufacturer’s  representative`, `manufacturer’s representative` (colliders get a `-<g18-id>` suffix)
- `maximum-capacity-max` ← `maximum capacity (max)`, `maximum capacity, max` (colliders get a `-<g18-id>` suffix)
- `maximum-flowrate-stem-q-mathrm-max` ← `maximum flowrate(stem:[q_{\mathrm{max}}])`, `maximum flowrate,stem:[q_{\mathrm{max}}]` (colliders get a `-<g18-id>` suffix)
- `maximum-operating-speed-vmax` ← `maximum operating speed (vmax)`, `maximum operating speed, vmax` (colliders get a `-<g18-id>` suffix)
- `maximum-permissible-error-mpe` ← `maximum permissible error (mpe)`, `maximum permissible error mpe`, `maximum permissible error, mpe` (colliders get a `-<g18-id>` suffix)
- `maximum-permissible-error-of-a-measuring-instrument` ← `maximum permissible error (of a measuring  instrument)`, `maximum permissible error (of a measuring instrument)` (colliders get a `-<g18-id>` suffix)
- `maximum-safe-load-lim` ← `maximum safe load (lim)`, `maximum safe load, lim` (colliders get a `-<g18-id>` suffix)
- `maximum-tare-effect-t-t` ← `maximum tare effect (t = + …, t = – …)`, `maximum tare effect, t+, t–` (colliders get a `-<g18-id>` suffix)
- `measurement-error` ← `(measurement) error`, `measurement error` (colliders get a `-<g18-id>` suffix)
- `measurement-error-error-of-measurement-error` ← `measurement error (error of measurement, error)`, `measurement error error of measurement error` (colliders get a `-<g18-id>` suffix)
- `measurement-repeatability-repeatability` ← `measurement repeatability (repeatability)`, `measurement repeatability repeatability` (colliders get a `-<g18-id>` suffix)
- `measurement-reproducibility-reproducibility` ← `measurement reproducibility (reproducibility)`, `measurement reproducibility reproducibility` (colliders get a `-<g18-id>` suffix)
- `measurement-standard-etalon` ← `measurement standard etalon`, `measurement standard {etalon}` (colliders get a `-<g18-id>` suffix)
- `metrological-authority` ← `metrological  authority`, `metrological authority` (colliders get a `-<g18-id>` suffix)
- `minimum-capacity-min` ← `minimum capacity (min)`, `minimum capacity, min` (colliders get a `-<g18-id>` suffix)
- `minimum-flow-rate-qmin` ← `minimum flow rate (qmin)`, `minimum flow rate, qmin` (colliders get a `-<g18-id>` suffix)
- `minimum-flowrate-stem-q-min` ← `minimum flowrate (stem:[q_{\min}])`, `minimum flowrate,stem:[q_{\min}]` (colliders get a `-<g18-id>` suffix)
- `minimum-operating-speed-vmin` ← `minimum operating speed (vmin)`, `minimum operating speed, vmin` (colliders get a `-<g18-id>` suffix)
- `minimum-totalized-load-min` ← `minimum totalized load (σmin)`, `minimum totalized load, σmin` (colliders get a `-<g18-id>` suffix)
- `minimum-totalized-load-stem-sigma-min` ← `minimum totalized load(stem:[\sigma_{\min}])`, `minimum totalized load,stem:[\sigma_{\min}]` (colliders get a `-<g18-id>` suffix)
- `multiple-range-instrument` ← `multiple range instrument`, `multiple-range instrument` (colliders get a `-<g18-id>` suffix)
- `non-automatic-checking-facility-type-n` ← `non-automatic checking facility (type n)`, `non-automatic checking facility (type n)*` (colliders get a `-<g18-id>` suffix)
- `non-automatic-zero-setting-device` ← `non-automatic zero setting device`, `non-automatic zero-setting device` (colliders get a `-<g18-id>` suffix)
- `operational-test` ← `operational  test`, `operational test` (colliders get a `-<g18-id>` suffix)
- `permanent-magnetization-m` ← `(permanent) magnetization (m)`, `permanent)  magnetization (m)` (colliders get a `-<g18-id>` suffix)
- `pre-setting-device` ← `pre-setting  device`, `pre-setting device` (colliders get a `-<g18-id>` suffix)
- `rated-minimum-fill-minfill` ← `rated minimum fill (minfill)`, `rated minimum fill,  minfill` (colliders get a `-<g18-id>` suffix)
- `reference-height-h` ← `reference height (h)`, `reference height, h` (colliders get a `-<g18-id>` suffix)
- `reference-material-rm` ← `reference material (rm)`, `reference material rm` (colliders get a `-<g18-id>` suffix)
- `reference-quantity-value-reference-value` ← `reference quantity value reference value`, `reference quantity value {reference value}`, `reference quantity value; reference value` (colliders get a `-<g18-id>` suffix)
- `relative-error-of-indication` ← `relative error (of indication)`, `relative error of indication` (colliders get a `-<g18-id>` suffix)
- `reproducibility-condition-of-measurement-reproducibility-condition` ← `reproducibility condition of measurement (reproducibility condition)`, `reproducibility condition of measurement {reproducibility condition}` (colliders get a `-<g18-id>` suffix)
- `resolution-of-a-displaying-device` ← `resolution (of a displaying device)`, `resolution of a displaying device` (colliders get a `-<g18-id>` suffix)
- `scale-interval-d` ← `scale interval (d)`, `scale interval, d` (colliders get a `-<g18-id>` suffix)
- `self-indication-capacity` ← `self-indication  capacity`, `self-indication capacity` (colliders get a `-<g18-id>` suffix)
- `semi-automatic-zero-setting-device` ← `semi-automatic zero setting device`, `semi-automatic zero-setting device` (colliders get a `-<g18-id>` suffix)
- `sensitivity-to-non-uniformity-in-the-field-of-a-thermographic-instrument` ← `sensitivity to non-uniformity in the field of a  thermographic instrument`, `sensitivity to non-uniformity in the field of a thermographic instrument` (colliders get a `-<g18-id>` suffix)
- `significant-fault` ← `significant fault`, `significant fault)` (colliders get a `-<g18-id>` suffix)
- `software-identification` ← `software  identification`, `software identification` (colliders get a `-<g18-id>` suffix)
- `software-separation` ← `software  separation`, `software separation` (colliders get a `-<g18-id>` suffix)
- `supplementary-devices` ← `supplementary  devices`, `supplementary devices` (colliders get a `-<g18-id>` suffix)
- `type-specific-parameter` ← `type specific parameter`, `type-specific  parameter`, `type-specific parameter` (colliders get a `-<g18-id>` suffix)
- `uncertainty-of-the-pressure-measurement` ← `uncertainty of the  pressure measurement`, `uncertainty of the pressure measurement` (colliders get a `-<g18-id>` suffix)
- `weighing-results` ← `weighing  results`, `weighing results` (colliders get a `-<g18-id>` suffix)
- `working-pressure-pw` ← `working pressure pw`, `working pressure, pw` (colliders get a `-<g18-id>` suffix)
- `working-temperature-tw` ← `working temperature  tw`, `working temperature, tw` (colliders get a `-<g18-id>` suffix)
- `zero-setting-device` ← `zero setting device`, `zero-setting device` (colliders get a `-<g18-id>` suffix)
- `zero-tracking-device` ← `zero tracking device`, `zero-tracking device` (colliders get a `-<g18-id>` suffix)

## Source data integrity

The vocabulary repo's `datasets/g18-*` directories are read-only
from this script's perspective — they are **never modified**.
Re-running this migration regenerates `data/` deterministically.
