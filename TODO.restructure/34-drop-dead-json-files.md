# 34 — Drop dead JSON output files

## Finding
Four large JSON files in `web/src/data/` have zero or near-zero consumers:

| File | Size | Code consumers | Test consumers |
|------|------|----------------|----------------|
| `term-by-slug.json` | 33MB | 0 | 0 |
| `terms-medium.json` | 12MB | 0 | 0 |
| `harmonization.json` | 19MB | 0 | 1 (data-contracts) |
| `terms.json` | 24MB | 0 | 1 (data-contracts) |

Total: **88MB of dead weight** re-generated every export run.

## Done in this session
- Deleted `term-by-slug.json` (33MB) — wasn't even generated by current code
- Deleted `terms-medium.json` (12MB) + removed its generation from
  `scripts/export_for_vite.rb`
- 45MB savings per build, no behavior change

## Remaining (deferred)
- `harmonization.json` (19MB) — only used by data-contracts test schema
  validation. Could be replaced by validating harmonization-slim.json
  + spot-checking per-term detail files. Risk: test loses some coverage.
- `terms.json` (24MB) — only used by data-contracts test full-schema
  validation + cross-check with terms-slim. Could be replaced by
  iterating per-term detail files. Risk: more complex test setup.

Combined savings if these are also dropped: 43MB more (88MB total).

## Verification
- pnpm build succeeds (3173 pages, same as before)
- pnpm test 179/182 pass (3 env-dependent failures unchanged)
